apply plugin: 'kotlin'
apply plugin: 'kotlin-kapt'

dependencies {
    implementation(
            kotlin.stdlib,
            rx.rxKotlin,
            rx.rxAndroid,
            rx.rxJava2
    )

    testImplementation(
            junit.junit,
            mockito.core,
            mockito.inline
    )
}

sourceCompatibility = "7"
targetCompatibility = "7"
